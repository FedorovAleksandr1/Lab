#include <stdio.h>

int main()
{
    // 10 - 100
    // 6
    int num, num2, m;
    scanf("%d", &num);
    printf((num >= 10 && num <= 100) ? "true\n": "false\n");

    scanf(" %o", &num2);
    printf("%c %d", num2 ? 'Y':'N',(m>>21) %2 );


}
